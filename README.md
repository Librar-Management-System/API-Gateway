# -API-Gateway
This repository contains the API Gateway for the Library Management System (LMS). The gateway works as the single entry point for all client requests and routes them to the correct backend service.
